---Siver para resetaer la data y dejar el id en 1
1)TRUNCATE TABLE APL_TB_CATALOGOTIPO;

2)ALTER TABLE APL_TB_CATALOGOTIPO
  MODIFY IDCATALOGOTIPO GENERATED BY DEFAULT AS IDENTITY (RESTART START WITH 1);
  
  
 --Sirve para que el campo sea unico y no se repitan los valores 
 ALTER TABLE APL_TB_CATALOGOTIPO 
ADD CONSTRAINT UK_CATALOGOTIPO_ETIQUETA UNIQUE (IDETIQUETA);

 ALTER TABLE APL_TB_CATALOGO 
ADD CONSTRAINT UK_CATALOGO_ETIQUETA UNIQUE (IDETIQUETA);